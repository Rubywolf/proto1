<!DOCTYPE html>
<html>
  <head>
    <title >Eggpix -- <%= @title %></title>
    <%= render 'layouts/stylesheets' %>
  </head>
  <body>
    <div class="container">
     <section class="round">
     <%= render 'layouts/sitenav' %>
        <%= yield %>
      </section>
      <% debug(params) if !Rails.env.production? %>
    </div>
  </body>
</html>